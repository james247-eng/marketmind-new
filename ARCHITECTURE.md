# 🏗️ ARCHITECTURE DIAGRAM - HOW EVERYTHING CONNECTS

## Project Architecture Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                        YOUR APPLICATION                              │
│                   (marketmind-02.netlify.app)                        │
└─────────────────────────────────────────────────────────────────────┘
                                 │
                    ┌────────────┼────────────┐
                    │            │            │
        ┌───────────▼──┐  ┌─────▼────┐  ┌───▼──────────┐
        │  FRONTEND    │  │ BACKEND  │  │  STORAGE     │
        │  (React)     │  │(Functions)  │(Cloudflare R2)│
        └──────────────┘  └──────────┘  └──────────────┘
```

---

## 1️⃣ FRONTEND LAYER - What Users See

```
┌─────────────────────────────────────────────┐
│     YOUR BROWSER (http://localhost:5173)    │
├─────────────────────────────────────────────┤
│  React 18 Components:                       │
│  ├─ Landing Page                           │
│  ├─ Dashboard (content generation)         │
│  ├─ Settings                               │
│  ├─ Social Media Linking                   │
│  ├─ Business Profiles                      │
│  └─ Pricing/Subscription                   │
├─────────────────────────────────────────────┤
│  Environment Variables Loaded:              │
│  ├─ VITE_FIREBASE_API_KEY                  │
│  ├─ VITE_FACEBOOK_APP_ID                   │
│  ├─ VITE_YOUTUBE_CLIENT_ID                 │
│  └─ VITE_PAYSTACK_PUBLIC_KEY               │
└─────────────────────────────────────────────┘
         │                      │
         │ (login/signup)       │ (social auth)
         │                      │
         ▼                      ▼
    [Firebase Auth]        [OAuth Providers]
    (Google, Email)        (Facebook, YouTube, etc.)
```

**Build Process:**

```
npm run dev           npm run build
    ↓                    ↓
Vite Dev Server     Production Build
(http://localhost:5173)  →  dist/ folder
Auto-reload          Optimized JS, CSS, HTML
```

---

## 2️⃣ AUTHENTICATION FLOW

```
┌────────────────┐
│   User Opens   │
│   App          │
└────────┬───────┘
         │
         ▼
    ┌─────────────────────────────────┐
    │  Are you logged in?            │
    │  ├─ Check Firebase Auth        │
    │  └─ Load user from Firestore   │
    └────────┬────────────────────────┘
             │
     ┌───────┴────────┐
     │                │
  [YES]           [NO]
     │                │
     ▼                ▼
[Dashboard]    [Login/Signup Page]
     │                │
     │                ├─ Email/Password
     │                ├─ Google OAuth
     │                └─ (other methods)
     │                │
     │                ▼
     │           [Firebase Auth API]
     │                │
     │                ├─ Create account
     │                ├─ Save to Firestore
     │                └─ Return user token
     │                │
     └────────┬───────┘
              │
              ▼
         [Dashboard]
         (Logged in!)
```

---

## 3️⃣ SOCIAL MEDIA LINKING FLOW

```
┌──────────────────────────────────────────────────────────────┐
│  User Clicks "Connect Instagram"                             │
└──────┬───────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────┐
│  [Frontend] Generates OAuth request                          │
│  - Uses VITE_FACEBOOK_APP_ID (embedded from .env.local)     │
│  - Redirects to Facebook OAuth server                        │
│  - Includes state parameter (security)                       │
└──────┬───────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────┐
│  [Facebook/Instagram OAuth Server]                           │
│  - User logs in                                              │
│  - User authorizes permissions                               │
│  - Redirects back to: ...auth/facebook/callback              │
│  - Includes: authorization CODE                              │
└──────┬───────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────┐
│  [Frontend] OAuthCallback.jsx receives CODE                  │
│  - Extracts authorization code from URL                      │
│  - Calls backend oauth-exchange function                     │
│  - Passes code to backend                                    │
└──────┬───────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────┐
│  [Backend] netlify/functions/oauth-exchange.js               │
│  - Receives authorization code                               │
│  - Uses FACEBOOK_APP_SECRET (from Netlify env)               │
│  - Calls Facebook API to exchange code for TOKEN             │
│  - Returns token to frontend                                 │
└──────┬───────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────┐
│  [Frontend] Stores token in Firestore                        │
│  - Encrypts token                                            │
│  - Saves to /socialAccounts/{userId}/facebook               │
│  - Shows "Connected!" message                                │
└──────┬───────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────┐
│  [Dashboard] User can now:                                   │
│  - Post to Instagram via MarketMind                          │
│  - Schedule content                                          │
│  - Analytics                                                 │
└──────────────────────────────────────────────────────────────┘
```

**⚠️ Why it might not work:**

```
1. ❌ VITE_FACEBOOK_APP_ID not set in Netlify env
   → OAuth won't initialize

2. ❌ Set env var but didn't trigger Netlify deploy
   → Code was built with old env vars
   → Must deploy to load new vars

3. ❌ Redirect URI not registered with Facebook
   → Facebook rejects redirect to your app

4. ❌ Backend secret (FACEBOOK_APP_SECRET) not in Netlify
   → oauth-exchange.js can't exchange code for token
```

---

## 4️⃣ CONTENT GENERATION FLOW

```
┌──────────────────────────────────────────────────────────────┐
│  User Clicks "Generate Content"                              │
│  Fills form:                                                 │
│  - Topic: "AI trends in 2024"                                │
│  - Length: "Medium"                                          │
│  - Tone: "Professional"                                      │
│  - Platform: "LinkedIn"                                      │
└──────┬───────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────┐
│  [Frontend] Calls Cloud Function                             │
│  - POST to /functions/generateContent                        │
│  - Passes: topic, length, tone, platform                     │
│  - User auth token (Firebase)                                │
└──────┬───────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────┐
│  [Backend] functions/index.js                                │
│  - Authenticates user (Firebase token)                       │
│  - Checks subscription tier (usage limits)                   │
│  - Calls Gemini AI API                                       │
│  - Uses GEMINI_API_KEY (from Netlify env)                    │
│  - Gemini generates content                                  │
└──────┬───────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────┐
│  [Backend] Processes Generated Content                       │
│  - Format for platform (LinkedIn hashtags, etc.)             │
│  - Create file (JSON/CSV/PDF)                                │
│  - Upload to Cloudflare R2                                   │
│  - Uses R2_ACCESS_KEY_ID & R2_SECRET_ACCESS_KEY              │
└──────┬───────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────┐
│  [Cloudflare R2] Stores File                                 │
│  - File location: /uploads/{userId}/{filename}               │
│  - Bucket: marketmind-content                                │
│  - Returns public URL                                        │
└──────┬───────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────┐
│  [Backend] Saves to Firestore                                │
│  - Document: /userContent/{userId}/{contentId}               │
│  - Fields: title, content, r2Url, createdAt, etc.            │
└──────┬───────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────┐
│  [Frontend] Shows Content to User                            │
│  - Preview                                                   │
│  - Download button (links to R2)                             │
│  - Post to platform button                                   │
│  - Schedule button                                           │
└──────────────────────────────────────────────────────────────┘
```

---

## 5️⃣ SUBSCRIPTION & PAYMENT FLOW

```
┌──────────────────────────────────────────────────────────────┐
│  User Clicks "Upgrade to PRO"                                │
└──────┬───────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────┐
│  [Frontend] Shows Paystack Payment Form                      │
│  - Uses VITE_PAYSTACK_PUBLIC_KEY (embedded)                  │
│  - Shows: Amount (₦9,999), description, user email           │
│  - Paystack SDK initializes                                  │
└──────┬───────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────┐
│  [User] Enters payment method                                │
│  - Card, Bank Transfer, USSD, Wallet, etc.                   │
│  - Completes payment                                         │
└──────┬───────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────┐
│  [Paystack] Processes Payment                                │
│  - Verifies payment success                                  │
│  - Returns reference ID                                      │
└──────┬───────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────┐
│  [Backend] Verifies with Paystack                            │
│  - Uses PAYSTACK_SECRET_KEY (Netlify env)                    │
│  - Confirms payment was successful                           │
│  - Creates subscription document                             │
└──────┬───────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────┐
│  [Firestore] Updates User                                    │
│  - Document: /subscriptions/{userId}                         │
│  - Fields: tier="PRO", expiresAt, paymentRef, etc.           │
│  - Resets usage limits for new tier                          │
└──────┬───────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────┐
│  [Frontend] Dashboard Updates                                │
│  - Shows "PRO" badge                                         │
│  - Unlocks premium features                                  │
│  - Updates usage limits                                      │
└──────────────────────────────────────────────────────────────┘
```

---

## 6️⃣ ENVIRONMENT VARIABLES DURING BUILD

```
LOCAL DEVELOPMENT (npm run dev):
┌─────────────────────────┐
│    .env.local          │
│  (your local file)      │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│  Vite loads variables                  │
│  Creates in-memory bundle               │
└────────┬────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│  http://localhost:5173                 │
│  VITE_FIREBASE_API_KEY=actual_value    │  (hardcoded in JS)
│  VITE_FACEBOOK_APP_ID=123456789        │  (embedded in bundle)
└─────────────────────────────────────────┘

PRODUCTION (npm run build + Netlify):
┌─────────────────────────┐
│  Netlify Environment    │
│  Variables (encrypted)  │
└────────┬────────────────┘
         │
         ▼
┌────────────────────────────────────────────┐
│  Netlify runs: npm run build              │
│  Vite replaces variables                  │
│  Creates dist/ folder                    │
└────────┬───────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────────┐
│  https://marketmind-02.netlify.app         │
│  VITE_FIREBASE_API_KEY=actual_value        │  (hardcoded in JS)
│  VITE_FACEBOOK_APP_ID=123456789            │  (embedded in bundle)
└──────────────────────────────────────────────┘

⚠️ KEY POINT:
- Variables are REPLACED during build with actual values
- They become hardcoded in the JavaScript bundle
- Changing Netlify env var doesn't affect already-built code
- Must TRIGGER DEPLOY after changing env vars
- This is why social linking failed without redeploy
```

---

## 7️⃣ FILE UPLOAD TO R2 FLOW

```
┌──────────────────────────────┐
│  Content Generation Done     │
│  (File ready to save)        │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│  [Backend] Cloud Function                │
│  - Have: generated content data          │
│  - Need: Upload to R2                    │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│  [Backend] Prepares Credentials          │
│  - R2_ACCESS_KEY_ID (from Netlify env)   │
│  - R2_SECRET_ACCESS_KEY (from Netlify env) │
│  - R2_BUCKET_NAME (from Netlify env)     │
│  - VITE_R2_ACCOUNT_ID (from Netlify env) │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│  [Backend] Uploads to R2 via API         │
│  - Endpoint: https://{accountId}.r2.dev  │
│  - Method: PUT /bucket/path/file         │
│  - Auth: Signature with secret key       │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│  [R2] Stores File                        │
│  - Path: uploads/{userId}/{filename}     │
│  - Status: ✅ Success                    │
│  - Public URL: Ready                     │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│  [Backend] Saves to Firestore            │
│  - Collection: /userContent/{userId}     │
│  - Field: r2_url = public R2 URL         │
│  - User can download anytime             │
└──────┬───────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────┐
│  [Frontend] Shows Download Button        │
│  - Links to R2 public URL                │
│  - User clicks → File downloads          │
└──────────────────────────────────────────┘
```

---

## 8️⃣ DEPLOYMENT SEQUENCE

```
YOU               GITHUB            NETLIFY             FIREBASE
 │                  │                  │                    │
 ├─ git push ──────▶│                  │                    │
 │                  │                  │                    │
 │                  ├─ webhook ───────▶│                    │
 │                  │                  │                    │
 │                  │                  ├─ npm install       │
 │                  │                  ├─ npm run build     │
 │                  │                  ├─ Vite embeds env   │
 │                  │                  │  vars from Netlify │
 │                  │                  │                    │
 │                  │                  ├─ Deploy dist/      │
 │                  │                  │  to CDN            │
 │                  │                  │                    │
 │                  │                  ├─ Deploy functions ─▶│
 │                  │                  │                    │
 │                  │                  ├─ Status: Publish   │
 │                  │                  │  ✅                 │
 │                  │◀─────────────────┤                    │
 │◀─────────────────┤                  │                    │
 │                  │                  │                    │
 └─ User visits site────────────────────────────────────────▶
    https://marketmind-02.netlify.app
```

---

## ENVIRONMENT VARIABLE REFERENCE

### Public Variables (Safe to show in JavaScript)

```
VITE_FIREBASE_API_KEY ← Embedded in frontend
VITE_FIREBASE_AUTH_DOMAIN
VITE_FIREBASE_PROJECT_ID
VITE_FIREBASE_STORAGE_BUCKET
VITE_FIREBASE_MESSAGING_SENDER_ID
VITE_FIREBASE_APP_ID
VITE_FACEBOOK_APP_ID
VITE_INSTAGRAM_APP_ID
VITE_YOUTUBE_CLIENT_ID
VITE_TIKTOK_CLIENT_KEY
VITE_TWITTER_API_KEY
VITE_PAYSTACK_PUBLIC_KEY
VITE_R2_ACCOUNT_ID
```

### Secret Variables (Only in backend)

```
GEMINI_API_KEY ← Only in Cloud Functions
FACEBOOK_APP_SECRET
FACEBOOK_CLIENT_ID
YOUTUBE_CLIENT_SECRET
TIKTOK_CLIENT_SECRET
TWITTER_API_SECRET
PAYSTACK_SECRET_KEY
R2_ACCESS_KEY_ID
R2_SECRET_ACCESS_KEY
R2_BUCKET_NAME
```

---

## SUMMARY

```
┌─────────────────────────────────────────────────────┐
│  Your App = Frontend + Backend + Storage            │
│                                                     │
│  Frontend (React):                                  │
│  ├─ Runs in user's browser                         │
│  ├─ Uses VITE_ variables (embedded)                │
│  ├─ Calls backend APIs                             │
│  └─ Shows UI                                       │
│                                                     │
│  Backend (Cloud Functions):                        │
│  ├─ Runs on Netlify/Firebase                       │
│  ├─ Uses secret variables                          │
│  ├─ Handles OAuth, payments, AI                    │
│  └─ Stores data in Firestore                       │
│                                                     │
│  Storage (Cloudflare R2):                          │
│  ├─ Stores user files                              │
│  ├─ Provides download URLs                         │
│  └─ Integrated with backend                        │
│                                                     │
│  Database (Firestore):                             │
│  ├─ Stores users, content, subscriptions           │
│  ├─ Real-time updates to frontend                  │
│  └─ Syncs across all sessions                      │
└─────────────────────────────────────────────────────┘
```

---

**Now you understand the full flow! 🎉**
